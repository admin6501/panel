# 🛡️ WireGuard Panel

یک پنل مدیریت WireGuard VPN کامل با قابلیت‌های پیشرفته

## ✨ ویژگی‌ها

### 🔐 سیستم احراز هویت
- سه سطح دسترسی: Super Admin، Admin، Viewer
- مدیریت چندین کاربر با سطوح مختلف
- احراز هویت JWT امن

### 👥 مدیریت کلاینت‌ها
- ایجاد، ویرایش، حذف کلاینت‌ها
- محدودیت حجم دانلود (Data Limit)
- محدودیت زمانی (Expiry Date)
- تولید QR Code برای اسکن با اپ WireGuard
- دانلود فایل کانفیگ
- فعال/غیرفعال کردن کلاینت‌ها
- ریست مصرف داده

### 📊 داشبورد
- نمایش آمار کلی کلاینت‌ها
- وضعیت آنلاین/آفلاین
- مصرف کل داده
- وضعیت سیستم و WireGuard

### 🌍 دو زبانه
- فارسی (RTL)
- انگلیسی (LTR)

### 📱 ریسپانسیو
- طراحی کاملاً ریسپانسیو برای موبایل، تبلت و دسکتاپ

### 🎨 رابط کاربری مدرن
- طراحی Dark Mode
- انیمیشن‌های روان
- رابط کاربری زیبا و کاربرپسند

## 🚀 نصب سریع

### پیش‌نیازها
- سرور با Ubuntu 20.04+ یا Debian 10+
- دسترسی Root

### نصب با یک دستور

```bash
bash <(curl -Ls https://raw.githubusercontent.com/your-repo/wireguard-panel/main/install.sh)
```

یا دانلود و اجرا:

```bash
wget -O install.sh https://raw.githubusercontent.com/your-repo/wireguard-panel/main/install.sh
chmod +x install.sh
sudo ./install.sh
```

## 🐳 نصب با Docker

### 1. کلون کردن پروژه

```bash
git clone https://github.com/your-repo/wireguard-panel.git
cd wireguard-panel
```

### 2. ایجاد فایل .env

```bash
cd docker
cp .env.example .env
# ویرایش فایل .env و تنظیم JWT_SECRET
nano .env
```

### 3. اجرا با Docker Compose

```bash
docker compose up -d
```

## ⚙️ تنظیمات

### متغیرهای محیطی Backend

| متغیر | توضیح | پیش‌فرض |
|-------|-------|---------|
| MONGO_URL | آدرس MongoDB | mongodb://localhost:27017 |
| DB_NAME | نام دیتابیس | wireguard_panel |
| JWT_SECRET | کلید رمزنگاری JWT | - |
| JWT_EXPIRATION_HOURS | مدت اعتبار توکن | 24 |
| WG_INTERFACE | نام اینترفیس WireGuard | wg0 |
| WG_PORT | پورت WireGuard | 51820 |
| WG_NETWORK | شبکه WireGuard | 10.0.0.0/24 |
| WG_DNS | سرورهای DNS | 1.1.1.1,8.8.8.8 |

## 📝 استفاده

### اولین ورود

1. به آدرس پنل بروید: `http://your-server-ip`
2. با اطلاعات پیش‌فرض وارد شوید:
   - نام کاربری: `admin`
   - رمز عبور: `admin`
3. **فوراً رمز عبور را تغییر دهید!**

### تنظیم Server Endpoint

1. به بخش تنظیمات بروید
2. آدرس سرور (IP یا دامنه) را وارد کنید
3. ذخیره کنید

### ایجاد کلاینت

1. به بخش کلاینت‌ها بروید
2. روی "افزودن کلاینت" کلیک کنید
3. اطلاعات را وارد کنید:
   - نام کلاینت
   - محدودیت حجم (اختیاری)
   - تاریخ انقضا (اختیاری)
4. ذخیره کنید
5. QR Code را اسکن کنید یا فایل کانفیگ را دانلود کنید

## 🔧 دستورات مفید

### مشاهده لاگ‌ها

```bash
# همه لاگ‌ها
docker compose logs -f

# فقط Backend
docker compose logs -f backend

# فقط Frontend
docker compose logs -f frontend
```

### ریستارت سرویس‌ها

```bash
docker compose restart
```

### بروزرسانی

```bash
git pull
docker compose down
docker compose up -d --build
```

### وضعیت WireGuard

```bash
wg show
```

## 🏗️ ساختار پروژه

```
wireguard-panel/
├── backend/
│   ├── server.py        # FastAPI main app
│   ├── models.py        # Data models
│   ├── auth.py          # Authentication
│   ├── wireguard.py     # WireGuard manager
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/  # React components
│   │   ├── pages/       # Page components
│   │   ├── contexts/    # React contexts
│   │   ├── i18n/        # Translations
│   │   └── utils/       # Utility functions
│   └── package.json
├── docker/
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend
│   ├── nginx.conf
│   └── docker-compose.yml
└── install.sh           # Auto installer
```

## 🔒 امنیت

- همه رمزهای عبور با bcrypt هش می‌شوند
- توکن‌های JWT با الگوریتم HS256 رمزنگاری می‌شوند
- دسترسی‌ها بر اساس نقش کاربر کنترل می‌شوند
- کلیدهای خصوصی WireGuard به صورت امن ذخیره می‌شوند

## 🤝 مشارکت

مشارکت شما را خوش‌آمد می‌گوییم! لطفاً Pull Request ارسال کنید.

## 📄 مجوز

MIT License

## 📞 پشتیبانی

در صورت بروز مشکل، یک Issue در GitHub ایجاد کنید.

---

ساخته شده با ❤️
